import { Component } from '@angular/core';
import log from 'loglevel';

@Component({
    selector: 'hs-kunde-footer',
    template: '',
})
export class FooterComponent {
    constructor() {
        log.debug('Footermodule.constructor()');
    }
}
